---
description: "Refresh Embody after committing externalized files so the manager UI's uncommitted (orange) badges reflect the new committed state"
---
<!-- Generated by Embody/Envoy - Do not remove this comment -->
# Refresh Embody After Committing

Embody's manager list shows an **orange "uncommitted"** badge for any externalized
file (`.tox`, `.tdn`, `.py`, `.glsl`, `.json`, ...) that is saved to disk but not
yet committed to git. That status is recomputed on Embody's refresh sweep, so a
commit does NOT clear the orange badges on its own -- something has to trigger a
refresh.

**After any successful `git commit` that includes externalized files, trigger an
Embody refresh** so the manager UI reflects the newly committed state immediately:

- Preferred (via MCP): call `execute_python` with `op.Embody.par.Refresh.pulse()`.
- Reference the extension inline; never cache `op.Embody.ext...` in a variable.

This keeps the orange -> clean (saved color) transition visible without a manual
refresh. Skip it only when the commit touched no externalized files.
