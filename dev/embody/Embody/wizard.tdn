format: tdxn
version: '2.0'
build: 24
generator: Embody/6.2.4
td_build: 099.2025.33070
source_file: Embody-6.307.toe
exported_at: '2026-08-30T08:23:44Z'
network_path: /embody/Embody/wizard
options:
  include_dat_content: false
  include_storage: true
type_defaults:
  buttonCOMP:
    parameters:
      bgalpha: 1
      clone: =op.TDTox.op('defaultCOMPs/button')
      hmode: fill
      repocomp: =me
      resizecomp: =me
    size: [160, 134]
  containerCOMP:
    parameters:
      repocomp: =me
      resizecomp: =me
    color: [0.67, 0.67, 0.67]
  outCHOP:
    parameters:
      autoexportroot: =parent()
      label: =me.name
    flags:
    - viewer
    size: [130, 90]
    color: [0.67, 0.67, 0.67]
  panelexecuteDAT:
    parameters:
      fromop: =parent()
  parameterCHOP:
    parameters:
      autoexportroot: =parent()
      builtin: true
      custom: false
      parameters: value0
      renameto: v1
    flags:
    - viewer
    size: [130, 90]
    color: [0.67, 0.67, 0.67]
  parameterexecuteDAT:
    parameters:
      defaultreadencoding: cp1252
      fromop: =parent()
      loadonstart: true
      onpulse: false
      op: ..
      pars: value0
    flags:
    - viewer
    size: [128, 66]
    color: [0.67, 0.67, 0.67]
  textCOMP:
    parameters:
      clickthrough: true
      repocomp: =me
      resizecomp: =me
    color: [0.67, 0.67, 0.67]
  textDAT:
    parameters:
      language: python
type: containerCOMP
parameters:
  w: 500
  h: 520
  resizecomp: =me
  repocomp: =me
  bgcolorr: 0.11
  bgcolorg: 0.12
  bgcolorb: 0.115
  bgalpha: 1
  align: verttb
  spacing: 10
color: [0.3, 0.5, 0.9]
tags:
- tdn
storage:
  sel:
    mode: advanced
  step: 0
operators:
- name: hint
  type: textCOMP
  parameters:
    type: multiline
    wordwrap: true
    callbacks: hint_callbacks
    fontsize: 11
    fontcolorr: 0.6
    fontcolorg: 0.61
    fontcolorb: 0.6
    textoffsetx: 24
    textpaddingr: 20
    alignx: left
    aligny: top
    w: 452
    alignorder: 3
    bgcolorr: 0.11
    bgcolorg: 0.12
    bgcolorb: 0.115
    bgalpha: 1
  size: [160, 130]
- name: logic
  type: textDAT
  parameters:
    file: embody/Embody/wizard/logic.py
    syncfile: true
  position: [0, 200]
  size: [130, 90]
  color: [0.9, 0.3, 0.4]
  tags:
  - py
- name: title
  type: textCOMP
  parameters:
    wordwrap: true
    callbacks: title_callbacks
    fontsize: 18
    fontcolorr: 0.92
    fontcolorg: 0.92
    fontcolorb: 0.92
    textoffsetx: 24
    alignx: left
    h: 30
    hmode: fill
    alignorder: 2
    bgcolorr: 0.11
    bgcolorg: 0.12
    bgcolorb: 0.115
    bgalpha: 1
  position: [480, 0]
  size: [160, 130]
- name: track
  type: containerCOMP
  parameters:
    h: 4
    hmode: fill
    alignorder: 1
    bgcolorr: 0.2
    bgcolorg: 0.21
    bgcolorb: 0.205
    bgalpha: 1
    marginl: 24
    marginr: 24
  position: [0, -150]
  size: [160, 130]
  children:
  - name: fill
    type: containerCOMP
    parameters:
      h: 4
      bgcolorr: 0.24
      bgcolorg: 0.52
      bgcolorb: 0.35
      bgalpha: 1
    size: [160, 130]
- name: clicks
  type: panelexecuteDAT
  parameters:
    panels: footer/btn_* grp_save/opt_* grp_mode/opt_* grp_assistant/opt_* grp_client/opt_* grp_permissions/opt_* grp_footprint/opt_* grp_git/opt_* grp_externalize/opt_* grp_convoy/opt_*
    ontooff: true
    file: embody/Embody/wizard/clicks.py
    syncfile: true
  position: [240, -150]
  size: [130, 90]
  color: [0.9, 0.3, 0.4]
  tags:
  - py
- name: footer
  type: containerCOMP
  parameters:
    h: 40
    hmode: fill
    bottomanchor: 0.923
    alignorder: 5
    bgcolorr: 0.11
    bgcolorg: 0.12
    bgcolorb: 0.115
    bgalpha: 1
    align: horizlr
    spacing: 10
    marginl: 24
    marginr: 24
    margint: 4
  position: [250, -700]
  size: [160, 130]
  children:
  - name: btn_back
    type: buttonCOMP
    parameters:
      buttontype: momentary
      h: 36
    children:
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 13
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        bgalpha: 1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        leftborder: bordera
        rightborder: bordera
        bottomborder: bordera
        topborder: bordera
        borderover: false
      flags:
      - viewer
      position: [74, 26]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: btn_next
    type: buttonCOMP
    parameters:
      buttontype: momentary
      h: 36
    position: [400, 0]
    children:
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 13
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        bgalpha: 1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        leftborder: bordera
        rightborder: bordera
        bottomborder: bordera
        topborder: bordera
        borderover: false
      flags:
      - viewer
      position: [74, 26]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
- name: spacer
  type: containerCOMP
  parameters:
    hmode: fill
    vmode: fill
    alignorder: 4.5
    bgcolorr: 0.11
    bgcolorg: 0.12
    bgcolorb: 0.115
    bgalpha: 1
  position: [0, -300]
  size: [160, 130]
- name: divider
  type: containerCOMP
  parameters:
    h: 1
    hmode: fill
    alignorder: 4.9
    bgcolorr: 0.24
    bgcolorg: 0.25
    bgcolorb: 0.245
    bgalpha: 1
    marginl: 24
    marginr: 24
  position: [240, -300]
  size: [160, 130]
- name: grp_git
  type: containerCOMP
  parameters:
    h: 235
    hmode: fill
    alignorder: 4
    bgcolorr: 0.11
    bgcolorg: 0.12
    bgcolorb: 0.115
    bgalpha: 1
    align: verttb
    spacing: 8
    marginl: 24
    marginr: 24
  position: [1715, -800]
  size: [160, 130]
  children:
  - name: opt_gitinit
    type: buttonCOMP
    parameters:
      value0: true
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Create a repo at the project root - recommended.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 24
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [200, 200]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [0, 200]
    - name: text
      type: textCOMP
      parameters:
        text: Initialize Git
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [200, 0]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [400, 200]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [400, 0]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_gitskip
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      alignorder: 1
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -200]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Work without version control. Add it later via op.Embody.InitGit().
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 24
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [200, 200]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [0, 200]
    - name: text
      type: textCOMP
      parameters:
        text: Skip for now
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [200, 0]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [400, 200]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [400, 0]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
- name: grp_mode
  type: containerCOMP
  parameters:
    h: 235
    hmode: fill
    alignorder: 4
    bgcolorr: 0.11
    bgcolorg: 0.12
    bgcolorb: 0.115
    bgalpha: 1
    align: verttb
    spacing: 8
    marginl: 24
    marginr: 24
  position: [450, -800]
  size: [160, 130]
  children:
  - name: opt_auto
    type: buttonCOMP
    parameters:
      value0: true
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Manages everything on its own. Recommended for most projects.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 24
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: Auto
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [74, 26]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_advanced
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      alignorder: 1
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -200]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Asks before touching git, your Python env, config files, or your network.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 24
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: Advanced
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [74, 26]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
- name: grp_save
  type: containerCOMP
  parameters:
    h: 190
    hmode: fill
    alignorder: 4
    bgcolorr: 0.11
    bgcolorg: 0.12
    bgcolorb: 0.115
    bgalpha: 1
    align: verttb
    spacing: 8
    marginl: 24
    marginr: 24
  position: [1455, -1060]
  size: [160, 130]
  children:
  - name: opt_savenow
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Opens the save dialog so everything lands beside your .toe.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 24
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [200, 200]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [0, 200]
    - name: text
      type: textCOMP
      parameters:
        text: Save the project now...
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [200, 0]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [400, 200]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [400, 0]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
- name: botspacer
  type: containerCOMP
  parameters:
    h: 26
    hmode: fill
    alignorder: 6
    bgcolorr: 0.11
    bgcolorg: 0.12
    bgcolorb: 0.115
    bgalpha: 1
  position: [0, -450]
  size: [160, 130]
- name: steplabel
  type: textCOMP
  parameters:
    callbacks: steplabel_callbacks
    fontsize: 11
    fontcolorr: 0.6
    fontcolorg: 0.61
    fontcolorb: 0.6
    textoffsetx: 24
    alignx: left
    h: 16
    hmode: fill
    bgcolorr: 0.11
    bgcolorg: 0.12
    bgcolorb: 0.115
    bgalpha: 1
    margint: 6
  position: [225, 200]
  size: [160, 130]
- name: topspacer
  type: containerCOMP
  parameters:
    h: 14
    hmode: fill
    alignorder: -1
    bgcolorr: 0.11
    bgcolorg: 0.12
    bgcolorb: 0.115
    bgalpha: 1
  position: [480, -450]
  size: [160, 130]
- name: grp_client
  type: containerCOMP
  parameters:
    w: 473
    h: 235
    alignorder: 4
    align: verttb
    spacing: 8
    marginl: 24
    marginr: 5
    pvscrollbar: 'on'
  position: [935, -800]
  size: [160, 102]
  children:
  - name: opt_codex
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 52
      alignorder: 2
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -400]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Generates Codex / AGENTS config.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 22
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: Codex
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [74, 26]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_cursor
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 52
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Generates .cursor config.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 22
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: Cursor
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [74, 26]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_gemini
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 52
      alignorder: 3
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -600]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Generates GEMINI.md.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 22
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: Gemini
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [74, 26]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_vscode
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 52
      alignorder: 4
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -800]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Generates VS Code MCP config.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 22
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: VS Code
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [74, 26]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_copilot
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 52
      alignorder: 5
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -1000]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Generates Copilot config.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 22
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: GitHub Copilot
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [74, 26]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_opencode
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 52
      alignorder: -1
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, 200]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Generates opencode.json config.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 22
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: OpenCode
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [74, 26]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_windsurf
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 52
      alignorder: 1
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -200]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Generates Windsurf config.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 22
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: Windsurf
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [74, 26]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
- name: grp_convoy
  type: containerCOMP
  parameters:
    h: 190
    hmode: fill
    alignorder: 4
    bgcolorr: 0.11
    bgcolorg: 0.12
    bgcolorb: 0.115
    bgalpha: 1
    align: verttb
    spacing: 8
    marginl: 24
    marginr: 24
  position: [1715, -1060]
  size: [160, 130]
  children:
  - name: opt_enable
    type: buttonCOMP
    parameters:
      value0: true
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Discover and control other Embody instances on your trusted LAN.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 24
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [200, 200]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [0, 200]
    - name: text
      type: textCOMP
      parameters:
        text: Enable Convoy
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [200, 0]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [400, 200]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [400, 0]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_disable
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      alignorder: 1
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -200]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Stay disconnected. Enable later on the Convoy parameter page.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 24
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [200, 200]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [0, 200]
    - name: text
      type: textCOMP
      parameters:
        text: Skip for now
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [200, 0]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [400, 200]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [400, 0]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
- name: grp_assistant
  type: containerCOMP
  parameters:
    h: 235
    hmode: fill
    alignorder: 4
    align: verttb
    spacing: 8
    marginl: 24
    marginr: 24
  position: [675, -800]
  size: [160, 130]
  children:
  - name: opt_none
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -200]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Convoy can still use its internal command service.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        textoffsety: -4
        alignx: left
        aligny: top
        h: 28
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: None - no AI assistant
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textoffsety: -4
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [225, 25]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_other
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -400]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: OpenCode, Cursor, Windsurf, Codex, Gemini, VS Code, or Copilot.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        textoffsety: -4
        alignx: left
        aligny: top
        h: 28
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: Other AI tool
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textoffsety: -4
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [250, 25]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_claudecode
    type: buttonCOMP
    parameters:
      value0: true
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Recommended. Generates .mcp.json plus .claude rules and skills.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        textoffsety: -4
        alignx: left
        aligny: top
        h: 28
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: Claude Code
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textoffsety: -4
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [250, 25]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
- name: grp_footprint
  type: containerCOMP
  parameters:
    h: 191
    hmode: fill
    alignorder: 4
    align: verttb
    spacing: 8
    marginl: 24
    marginr: 24
  position: [1195, -800]
  size: [160, 130]
  children:
  - name: opt_custom
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Pick a folder next.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 28
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: Custom folder
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [74, 26]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_gitroot
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -200]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Write config at the top of the git repo.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 28
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: Git root (recommended)
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [74, 26]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_projectfolder
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -400]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Use when the .toe is a subfolder you open as your workspace.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 28
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: Project folder
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [74, 26]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [66, -83]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [247, -81]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
- name: hint_callbacks
  type: textDAT
  flags:
  - viewer
  position: [190, 0]
  size: [160, 130]
  color: [0.67, 0.67, 0.67]
  dock: hint
  dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
  dat_content_format: text
- name: grp_externalize
  type: containerCOMP
  parameters:
    h: 235
    hmode: fill
    alignorder: 4
    bgcolorr: 0.11
    bgcolorg: 0.12
    bgcolorb: 0.115
    bgalpha: 1
    align: verttb
    spacing: 8
    marginl: 24
    marginr: 24
  position: [1715, -1400]
  size: [160, 130]
  children:
  - name: opt_auto
    type: buttonCOMP
    parameters:
      value0: true
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      alignorder: 1
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -200]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Externalize scripts and COMPs as you create them. Nothing changes today.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 24
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [200, 200]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [0, 200]
    - name: text
      type: textCOMP
      parameters:
        text: New work only
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [200, 0]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [400, 200]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [400, 0]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_full
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Write the whole project to disk now, and keep new work externalized.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 24
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [200, 200]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [0, 200]
    - name: text
      type: textCOMP
      parameters:
        text: Externalize everything
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [200, 0]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [400, 200]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [400, 0]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_skip
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      alignorder: 2
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -400]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Leave externalization off. Turn it on later from the Embody parameters.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 24
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [200, 200]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [0, 200]
    - name: text
      type: textCOMP
      parameters:
        text: Not now
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [200, 0]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [400, 200]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [400, 0]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
- name: grp_permissions
  type: containerCOMP
  parameters:
    w: 473
    h: 190
    alignorder: 4
    align: verttb
    spacing: 8
    marginl: 24
    marginr: 5
    pvscrollbar: auto
    scrolloverlay: ignore
  position: [1455, -800]
  size: [160, 130]
  children:
  - name: opt_all
    type: buttonCOMP
    parameters:
      value0: true
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Auto-approve all Envoy tools. No permission prompts.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 28
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: Don't ask (recommended)
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [225, 25]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [180, -180]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [330, -180]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -180]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_some
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      alignorder: 1
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -200]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Auto-approve read-only tools; prompt before changes.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 28
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: Ask for some
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [225, 25]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [180, -180]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [330, -180]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -180]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_leave
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      alignorder: 3
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -600]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Don't create or modify .claude/settings.local.json.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 28
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: Leave settings alone
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [225, 25]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [180, -180]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [330, -180]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -180]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
  - name: opt_prompt
    type: buttonCOMP
    parameters:
      buttontype: radiodown
      colorr: 0.2
      colorg: 0.42
      colorb: 0.28
      h: 58
      alignorder: 2
      bgcolorr: =0.16 + 0.35*int(me.par.value0)*(me.par.colorr - 0.16) + 0.03*me.panel.rollover
      bgcolorg: =0.17 + 0.35*int(me.par.value0)*(me.par.colorg - 0.17) + 0.03*me.panel.rollover
      bgcolorb: =0.165 + 0.35*int(me.par.value0)*(me.par.colorb - 0.165) + 0.03*me.panel.rollover
      borderar: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorr + .2
      borderag: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorg + .2
      borderab: =[c/.5 for c in [.23, .4, .3, .6]][me.par.value0 + 2*me.panel.rollover] * me.par.colorb + .2
      leftborder: bordera
      rightborder: bordera
      bottomborder: bordera
      topborder: bordera
      align: verttb
      spacing: 1
    position: [0, -400]
    children:
    - name: desc
      type: textCOMP
      parameters:
        text: Prompt before every Envoy tool. Most cautious.
        type: multiline
        wordwrap: true
        callbacks: desc_callbacks
        fontsize: 11
        fontcolorr: 0.62
        fontcolorg: 0.63
        fontcolorb: 0.62
        textoffsetx: 18
        alignx: left
        aligny: top
        h: 28
        hmode: fill
        alignorder: 1
      size: [160, 130]
    - name: out1
      type: outCHOP
      position: [275, 175]
      inputs:
      - par1
    - name: par1
      type: parameterCHOP
      position: [65, 175]
    - name: text
      type: textCOMP
      parameters:
        text: Ask for all
        type: multiline
        callbacks: text_callbacks
        legacyfontselection: true
        scaletofit: =parent().par[me.curPar.name] or me.curPar.val
        fontsize: 14
        linespacing: =parent().par[me.curPar.name] or me.curPar.val
        fontcolorr: 0.93
        fontcolorg: 0.93
        fontcolorb: 0.93
        textoffsetx: 16
        textpaddingl: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingr: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingb: =parent().par[me.curPar.name] or me.curPar.val
        textpaddingt: =parent().par[me.curPar.name] or me.curPar.val
        alignx: left
        aligny: bottom
        h: 28
        hmode: fill
        vmode: fill
        bgcolorr: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorr + r * 0.1
        bgcolorg: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorg + r * 0.1
        bgcolorb: =[c/.5 for c in [.2, .4, .15, .35]][parent().par.value0 + 2*(r:=parent().panel.rollover)] * parent().par.colorb + r * 0.1
        borderar: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorr + .2
        borderag: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorg + .2
        borderab: =[c/.5 for c in [.23, .4, .3, .6]][parent().par.value0 + 2*parent().panel.rollover] * parent().par.colorb + .2
        borderover: false
      flags:
      - viewer
      position: [225, 25]
      size: [113, 96]
    - name: parexec1
      type: parameterexecuteDAT
      position: [180, -180]
      dat_content: "\"\"\"\nParameter Execute DAT\n\nme - this DAT\n\nMake sure the corresponding toggle is enabled in the Parameter Execute DAT.\n\"\"\"\n\nfrom typing import Any, List\n\ndef onValueChange(par: Par, prev: Any):\n\t\"\"\"\n\tCalled when a parameter value changes.\n\t\n\tArgs:\n\t\tpar: The Par object that has changed (use par.eval() to get current)\n\t\tprev: The previous value\n\t\"\"\"\n\t# use par.eval() to get current value\n\treturn\n\ndef onValuesChanged(changes: List[ParChange]):\n\t\"\"\"\n\tCalled at end of frame with complete list of parameter changes.\n\t\n\tArgs:\n\t\tchanges: List of ParChange named tuples (Par, previous value)\n\t\"\"\"\n\tfor c in changes:\n\t\t# use par.eval() to get current value\n\t\tpar = c.par\n\t\tprev = c.prev\n\treturn\n\ndef onPulse(par: Par):\n\t\"\"\"\n\tCalled when a parameter is pulsed.\n\t\n\tArgs:\n\t\tpar: The Par object that was pulsed\n\t\"\"\"\n\treturn\n\ndef onExpressionChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter expression changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current expression value\n\t\tprev: The previous expression value\n\t\"\"\"\n\treturn\n\ndef onExportChange(par: Par, val: str, prev: str):\n\t\"\"\"\n\tCalled when a parameter export changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current export value\n\t\tprev: The previous export value\n\t\"\"\"\n\treturn\n\ndef onEnableChange(par: Par, val: bool, prev: bool):\n\t\"\"\"\n\tCalled when a parameter enable state changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current enable state\n\t\tprev: The previous enable state\n\t\"\"\"\n\treturn\n\ndef onModeChange(par: Par, val: ParMode, prev: ParMode):\n\t\"\"\"\n\tCalled when a parameter mode changes.\n\t\n\tArgs:\n\t\tpar: The Par object that changed\n\t\tval: The current ParMode\n\t\tprev: The previous ParMode\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: panelexec1
      type: panelexecuteDAT
      parameters:
        panels: =parent()
        panelvalue: state
        offtoon: false
        valuechange: true
        defaultreadencoding: cp1252
        loadonstart: true
      flags:
      - viewer
      position: [330, -180]
      size: [126, 62]
      color: [0.67, 0.67, 0.67]
      dat_content: "\"\"\"\nPanel Execute DAT\n\nme - this DAT\n\npanelValue - the PanelValue object that changed\nprev - the previous value of the PanelValue object that changed\n\nMake sure the corresponding toggle is enabled in the Panel Execute DAT.\n\"\"\"\n\nfrom typing import Any\n\ndef onOffToOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from 0 to non-zero.\n\t\"\"\"\n\treturn\n\ndef whileOn(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is non-zero.\n\t\"\"\"\n\treturn\n\ndef onOnToOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled when a panel value changes from non-zero to 0.\n\t\"\"\"\n\treturn\n\ndef whileOff(panelValue: PanelValue):\n\t\"\"\"\n\tCalled every frame while a panel value is 0.\n\t\"\"\"\n\treturn\n\ndef onValueChange(panelValue: PanelValue, prev: Any):\n\t\"\"\"\n\tCalled when a panel value changes.\n\t\n\tArgs:\n\t\tpanelValue: The PanelValue object that changed\n\t\tprev: The previous value of the PanelValue object\n\t\"\"\"\n\treturn\n"
      dat_content_format: text
    - name: desc_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -180]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: desc
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
    - name: text_callbacks
      type: textDAT
      flags:
      - viewer
      position: [0, -160]
      size: [160, 130]
      color: [0.67, 0.67, 0.67]
      dock: text
      dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
      dat_content_format: text
- name: title_callbacks
  type: textDAT
  flags:
  - viewer
  position: [480, -160]
  size: [160, 130]
  color: [0.67, 0.67, 0.67]
  dock: title
  dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
  dat_content_format: text
- name: steplabel_callbacks
  type: textDAT
  flags:
  - viewer
  position: [415, 200]
  size: [160, 130]
  color: [0.67, 0.67, 0.67]
  dock: steplabel
  dat_content: "# Text Component callbacks\n\nfrom typing import Any, Dict\n\ndef onValueChange(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled whenever the text component's value parameter changes for any reason\n\tincluding typing in the text window or changing the parameter directly. The\n\tcallback is equivalent to the onValueChange in the parameter exectute DAT.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonValueChange comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n\t\ndef onFocus(comp: textCOMP):\n\t\"\"\"\n\tCalled when one of the text components viewers gets keyboard focus. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonFocus comp:', comp.path)\n\treturn\n\ndef onFocusEnd(comp: textCOMP, info: Dict[str, str]):\n\t\"\"\"\n\tCalled when one of the text component's viewers loses keyboard focus. In\n\tregular editing mode this is right after the contents are saved back to the\n\tvalue parameter. This function is called everytime the viewer loses focus,\n\teven if the text does not change.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tinfo: a dictionary containing information about the event including:\n\t\t\treason: a string indicating what caused the viewer to lose focus\n\t\t\t\te.g. 'enter', 'escape', 'tab', 'unknown'\n\t\n\t\"\"\"\n\t#debug('\\nonFocusEnd comp:', comp.path, '- info:\\n', info)\n\treturn\n\t\ndef onTextEdit(comp: textCOMP):\n\t\"\"\"\n\tCalled each time the contents of the text component viewer change i.e. when\n\ttyping a character, deleting text, cutting or pasting. \n\t\n\tArgs:\n\t\tcomp: the text component\n\t\"\"\"\n\t#debug('\\nonTextEdit comp:', comp.path, '- text: ', comp.editText)\n\treturn\n\t\ndef onTextEditEnd(comp: textCOMP, value: str, prevValue: str):\n\t\"\"\"\n\tCalled when the user finishes editing and the value has been changed. Not\n\tcalled in 'Continuous Edit' mode. Use onTextEdit and onFocusEnd when using\n\tcontinuous editing.\n\n\tNote: Any Python undo blocks created by this callback will be included in\n\tthe undo step for the change to the text field.\n\t\n\tArgs:\n\t\tcomp: the text component\n\t\tvalue: the new value\n\t\tprevValue: the previous value\n\t\"\"\"\n\t#debug('\\nonTextEditEnd comp:', comp.path, '- new value: ', value,\n\t#\t  ', prev value: ', prevValue )\n\treturn\n"
  dat_content_format: text
