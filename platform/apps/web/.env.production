# Production build-time env (loaded by `astro build`, prod mode only -- not local dev).
# PUBLIC_ vars are exposed to the browser by design; this site key is public
# (it ships in the client HTML) and hostname-bound, so it is safe to commit.
# Read in submit.astro via import.meta.env.PUBLIC_TURNSTILE_SITE_KEY.
PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAADkqXek6hZkwxqTi
